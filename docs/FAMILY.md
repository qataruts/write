# عائلة تطبيقات التأسيس — الخطة العامة

**التاريخ**: ١١ أغسطس ٢٠٢٦ · **المصدر**: دراسة `ref/sonnet-con` (١٤ صفحة) + تجربة «اِقْرَأْ» المكتملة · **الحال**: **معتمدة** — أقرّ المالكُ (١١ أغسطس ٢٠٢٦) البنيةَ (مستودعٌ لكل تطبيق) والتسميةَ (ثلاث طبقات) وقرارَي التطبيقين المركزيين (أرقامُ احسب المشرقية · محرّكُ اكتب بتتبّع المسار).
**نسخةٌ مطابقة من هذا الملف في مستودعَي `calc` و`write`** — كلُّ تطبيقٍ مستقلٌّ يحمل خارطةَ العائلة كاملة.

## ١. الفكرة المركزية (من الدراسة، وأثبتتها اقرأ)

لا نبني تطبيقاتٍ منفصلة تتكرّر فيها الجهود، بل **محرّكاً تعليمياً واحداً** (فاحص محتوى آليّ، تكرارٌ متباعد، قياسٌ دقيق، خصوصيةٌ محلية كاملة) **يُعاد استعماله في كل مادة**. كلُّ تطبيقٍ تالٍ هو أساساً **عملُ محتوىً منهجيّ** فوق أساسٍ تقنيّ جاهز.

## ٢. الخارطة — مساران متوازيان، ستةُ تطبيقات (المستوى ١)

| # | التطبيق | المسار | الحال |
|---|---|---|---|
| ١ | **اِقْرَأْ** (read) | أ — عربي/قرآن | ✅ منجَز، حيّ على `read.mishkat.qa` |
| ٢ | **اِحْسِبْ** (calc) | ب — حساب | **يبدأ الآن** (بالتوازي) |
| ٣ | **اُكْتُبْ** (write) | أ — عربي | **يبدأ الآن** (بالتوازي) |
| ٤ | **أَتْقِنْ** (master) | أ — عربي | بعد اكتب — يحتاج محرّكَ كتابته |
| ٥ | **رَتِّلْ** (rattil) | أ — قرآن | يُكمل مسار أ |
| ٦ | الإنجليزية ١ (اسمه لاحقاً) | ب | بعد استقرار احسب |

- **مسار أ بالتسلسل**: كلُّ تطبيقٍ يعيد استعمال أصول سابقه مباشرة (ترتيب الحروف، بنوك الكلمات والجمل، الأصوات).
- **مسار ب بالتوازي**: لا يعتمد على مسار أ تقنياً ولا منهجياً — استقلالٌ يسمح بجلسات إدارةٍ وتنفيذٍ متوازية.
- المستويات ٢ و٣ (حتى الصف السادس، ١٨ تطبيقاً في الأفق) **أفقٌ معروف لا التزامٌ حاليّ**.

## ٣. المبادئ — ثمانيةٌ من الدراسة، وتاسعٌ من ميدان اقرأ

1. **مسارٌ واحد متصل** — محطاتٌ مقفلة تُفتح بالإتمام، لا قائمةَ اختيار.
2. **فاحصُ محتوىً آليّ** — قيدٌ برمجيّ يرفض أيَّ مادةٍ فيها عنصرٌ لم يُدرَّس، قبل أن تصل الطفل.
3. **الترتيبُ بالأثر لا بالتقليد** — يبدأ الطفل بما يعطيه أثراً حقيقياً سريعاً.
4. **تباعدُ المتشابهات ثم مواجهتُها قصداً**.
5. **تكرارٌ متباعد دقيق** (Leitner) على مستوى (الوحدة × الحالة × نوع التمرين)، بتسلسل ١←٢←٤←٨←١٦ يوماً.
6. **لا مؤقّت، لا عقاب، لا شاشة «خطأ»** — الخطأ يُسمَع/يُرى ليقارن، ثم يمضي.
7. **خصوصيةٌ مطلقة محلية** — لا حساب، لا تتبّع، لا سحابة؛ PWA يعمل دون إنترنت.
8. **شفافيةُ القرار المنهجي** — كلُّ مخالفةٍ للمألوف تُعلَن بسببها، وحدودُ النطاق مكتوبة.
9. **حراسةُ الغياب وبلاغُ الميدان** *(زيادةُ اقرأ)*: أربعةُ أصناف عيوبٍ لا يُمسكها اختبارٌ عاديّ تُغلَق بنيوياً من اليوم الأول — *رقمٌ مكتوب بيد في حارس · فحصٌ لا يُشغَّل · تدريسٌ بلا قياس · مُلتزَمٌ ليس منشوراً* — **وبلاغُ الطفل والمعلم الميدانيّ سيدُ الأدلة** (أثبت في اقرأ ١٠/١٠ أنّه يرى ما لا يراه عشرون حارساً).

## ٤. المعمارية والبنية التنفيذية (قرارات المالك، ١١ أغسطس ٢٠٢٦)

- **تطبيقُ PWA مستقلٌّ لكل مادة**: هويةٌ وأيقونةٌ وتخزينٌ (origin) منفصل — للخصوصية، وتجربة الطفل، والاعتماد المؤسسي، وخفّة التحميل (قرار الدراسة §٥).
- **مستودعُ git مستقلٌّ لكل تطبيق** (لا مونوريبو): `qataruts/read` قائمٌ، ويُنشأ `qataruts/calc` و`qataruts/write`. العلّة: جلساتُ تنفيذٍ متوازية بلا تصادم دفع، وخطُّ نشرٍ مستقل (Pages لكل مستودع)، وقاعدةُ اقرأ «لا npm ولا أدوات بناء» تُسقط منفعةَ المونوريبو الأساسية (إدارة الحزم المشتركة). نموذجُ IntentText مونوريبو لأنه حِزمٌ npm — وحالُنا غيرُ حاله.
- **البذرة تُنسخ ولا تُستورد**: كلُّ تطبيقٍ جديد يبدأ بنسخ «بذرة المنصة» من اقرأ (القائمة أدناه)، ويُقيَّد في `docs/SEED.md` **ما نُسخ ومن أيّ التزام** — فالاستخلاصُ اللاحق يعرف ما تفرّع. **ولا مكتبة مشتركة الآن**: الدراسة تنصّ على استخلاص المحرّك **بعد نضوج تطبيقين على الأقل** وتَعُدّ التجريد المبكر خطراً (over-engineering).
- **workspace لكل تطبيق**: `calc.code-workspace` و`write.code-workspace`، وكلٌّ منهما يضمّ مجلد `read` **مجلدَ مرجعٍ ثانياً** (تُقرأ أصولُه ولا تُمَسّ).
- **جلسةُ إدارةٍ (فابل ٥) لكل تطبيق** تملك `METHOD.md` و`SESSIONS.md` وتراجع المنتجات، و**جلساتُ تنفيذٍ (أوبوس ٥)** ببنود جلساتٍ محدودةِ الحدود بمعايير قبول — عينُ نموذج اقرأ الذي عمل.

## ٥. بذرةُ المنصة (ما يُنسخ من اقرأ إلى كل تطبيقٍ جديد)

**من `app/js/`** (المحايدُ للمادة): `progress.js` (الرحلة/الجبهة/النجوم/ليتنر/النسخ الاحتياطي/المعاينة) · `review.js` (محرّك جلسة المراجعة — هيكلُه لا تمارينُه) · `gate.js` · `parent.js` (البنية والبوابة الحسابية) · `audio.js` (بصمات + احتياط مضبوط) · `ui.js` (h/الأيقونات/faceEl/DEV) · هيكلُ `main.js` (الموجّه/الخريطة/الأدراج) · `sw.js` (v20: مخزنٌ ثابت، شفاء، شفافية تحميل) · لوحُ `app.css`.
**من `tools/`**: `browser_test.py` وعدَدُه · `test_selftests.mjs` · `test_measure.mjs` · `test_nodes.mjs` · `test_pwa.mjs` · `test_audio_cache.mjs` · `check_live.py` · `cdn_check.py` · `perf_check.py` · `generate_audio.py` + `audio_panel.py` + `trim_lead.py` · `make_icons.py` + `fetch_twemoji.py`.
**من `docs/`**: قوالبُ `AUDIO_QUEUE.md` (بروتوكول فصل الصوت) و`DESIGN.md` (أرقام المشرق، أهداف اللمس ٦٤بك، صدق الصورة) وبنيةُ `welcome/` الأربع + وضع المعاينة + قسم «ثبّته أولاً».
**لا يُنسخ**: `curriculum.js` وكلُّ بياناتِ ومحتوى وشاشات المادة — تلك تُكتب لكل تطبيقٍ من منهجه.

## ٦. النشر والتسمية (سياسةٌ واحدة، ثلاثُ طبقات)

| الطبقة | القاعدة | الأمثلة |
|---|---|---|
| تقنية (مجلد/مستودع/نطاق) | فعلٌ إنجليزيّ بسيط | `read` · `calc` · `write` · لاحقاً `master` · `rattil` |
| علامةُ الطفل | عربيةٌ مشكولة، **ولكل تطبيقٍ خطُّ علامته** (تصحيحُ ١٢ أغسطس أدناه) | اِقْرَأْ · اِحْسِبْ · اُكْتُبْ · أَتْقِنْ · رَتِّلْ |
| الاسمُ اللاتيني للعالمية | نقحرةٌ لمسار أ (Iqra·Uktub·Atqin·Rattil لقيمتها عند المسلمين غير العرب) وإنجليزيةٌ بسيطة لمسار ب (Calc·…) | توصية دراسة التسمية |

- النشر: `qataruts/<app>` ← GitHub Pages ← `<app>.mishkat.qa` خلف Cloudflare (CNAME جديد لكل تطبيق، برتقاليّ). **قاعدةُ `immutable-assets` تشمل النطاقات الفرعية الجديدة تلقائياً** — لأنها على مستوى المنطقة (`.mp3/.svg/.woff2` أياً كان النطاق الفرعي)، فحصةُ GitHub محميةٌ للعائلة كلِّها بلا إعدادٍ إضافيّ.
- الصوت: **سُلافات نفسُه للعائلة كلها** — هويةٌ صوتيةٌ واحدة يعرفها الطفل من تطبيقٍ إلى آخر (وتلاوةُ القرآن قارئٌ متقن حصراً حيث وُجدت).
- زرُّ تثبيتٍ برمجيّ (`beforeinstallprompt`) لأندرويد/الحاسوب + لافتةُ سفاري لآبل (ملف `instal-pwa.md`) — **يدخل بذرةَ العائلة، ويُعاد إلى اقرأ لاحقاً**.

## ٧. معيارُ أيّ مادةٍ جديدة (من الدراسة §٨)

> **هل الإجابةُ فيها قابلةٌ للفحص الآليّ بوضوح — صحيحة أو خاطئة، لا رأياً؟**
نعم ⇒ تناسب المحرك (قراءة، حساب، إملاء، تجويد، صوتيات إنجليزية). لا ⇒ تؤجَّل (إنشاء، علوم، تاريخ).

## ٨. حدودُ النطاق المعلنة

- تطبيقاتُ **تدريسٍ وقياس، لا تشخيص** — التعثّر المتكرر إشارةُ «راجع مختصاً»، لا حكماً.
- الفئةُ العمرية لكل تطبيقٍ محدّدةٌ ومعلنة، لا تُمدَّد ضمنياً.
- الإعداديُّ والعلومُ والموادُّ التحريرية مؤجَّلةٌ عمداً (معيار §٧).

## ٩. ميثاق الهوية — إخوةٌ لا توائم (أمر المالك، ١١ أغسطس ٢٠٢٦)

البذرةُ تنسخ الهيكلَ فيولد التطبيقُ بوجه أخيه — وذلك عيبُ ولادةٍ يُصلَح بقرار، لا
قدَرٌ يُقَرّ. لاحظ المالكُ أن احسب اختار لونه واكتب بقي بلون اقرأ، فسأل: أيصحّ أن
تتشابه التطبيقات هكذا؟ والجواب قاعدةً: **التشابه في الهيكل صحةٌ، والتشابه في
الهوية عيب** — العظامُ واحدة والوجهُ لكلٍّ.

**ما يُشترك بالبناء ولا يُمَسّ** (عظامُ العائلة):
- المحرّكُ والعهود: قناةُ الصوت وقواعدُها («لا انتقالَ وكلامٌ في الجوّ»)، ليتنر
  والبوابات، العملُ دون إنترنت، الخصوصيةُ المطلقة، فلسفةُ الحرّاس.
- قواعدُ التفاعل التربوية: الأزرارُ الكبيرة، «يسمع ما اختاره ولا يُلقَّن»،
  الخريطةُ والنجوم، لوحةُ الوالد، «لا تدريسَ بلا قياس».
- **الصوتُ واحد**: معلّمُ العائلة بصوته (سُلافات) في الموادّ كلها — طفلٌ ينتقل
  من تطبيقٍ إلى أخيه فيجد معلّمَه نفسَه، وهذا من الطمأنينة لا من الفقر.

**ما يفترق وجوباً** (وجهُ التطبيق — أربعةُ بنودٍ لكلٍّ):
1. **اللوح**: عائلةُ لونٍ غالبةٌ يملكها التطبيقُ وحدَه — لا يتشارك تطبيقان لوناً
   غالباً. (اقرأ ورقُه الدافئ ولوحُه القائم؛ واحسب لونُه الذي أقرّه المالك؛
   واكتب **مطالَبٌ باختيار لوحه** — ما ورثه من البذرة نائبٌ مؤقت لا قرار.)
2. **الأيقونة**: تُعرَف من لمحةٍ بين أخواتها — فالتطبيقات ستتجاور على شاشة آيبادٍ
   واحدة، وأيقونتان بلونٍ واحد عيبُ استعمالٍ لا ذوقٍ فحسب. ومحكُّها: صفُّ أيقونات
   العائلة معاً، وطفلُ سادسةٍ يسمّي كلَّ واحدةٍ بلا تردّد.
3. **الاستعارةُ البطلة**: عالمٌ يملكه التطبيق — اقرأ: الحروفُ والبساتين والمكتبة؛
   احسب: عالمُ العدّ؛ اكتب: القلمُ والأثر. منها تُشتقّ أسماءُ المحطات وصورُ
   الخريطة ونكهةُ الاحتفال — فالهيكلُ واحد والقصةُ التي يعيشها الطفل مختلفة.
4. **العلامة**: لكل تطبيقٍ رسمُ علامته وخطُّها قرارَ مراجعةٍ يُقَرّ — وخطُّ
   Marhey لعلامة اقرأ **وحدَها** فلا يُورَّث بالنسخ.

**الإجراء**:
- الهويةُ قرارُ مالكٍ كامتحان أيقونات اقرأ: صفحةُ `docs/REVIEW_IDENTITY.md` في
  كل تطبيقٍ (اللوح · الأيقونة · الاستعارة · النبرة) تُعرَض بمرشَّحاتٍ مُصيَّرة
  وتُقَرّ — لا لونَ يُختار وصفاً بلا عين.
- **نسخُ البذرة يحمل الهيكلَ لا الهوية**: كلُّ لونٍ أو علامةٍ ورثها تطبيقٌ من
  اقرأ نائبٌ مؤقتٌ يُستبدَل **قبل أول عرضٍ على طفل**.
- **حارسُ الهوية**: فحصٌ في كل تطبيقٍ يثبت أن قيمَ لوحه الأساسية (الورق والغالب)
  **تخالف** قيمَ اقرأ المعلومة — فالوجهُ المستعار يحمرّ يومَ يُنسى لا يومَ يُلاحَظ.

> **تصحيحُ تعارضٍ (حكم المالك، ١٢ أغسطس ٢٠٢٦ — جلسة إدارة احسب):** كان §٦ ينصّ
> «عربيةٌ مشكولة بخط Marhey» للعائلة كلها، و§٩.٤ يقول «Marhey لعلامة اقرأ وحدَها»
> — فتصادما، وحكم المالكُ أنّ **الميثاق يسود**: لكل تطبيقٍ خطُّ علامته قرارَ
> مراجعةٍ بمرشَّحاتٍ مُصيَّرة، وصُحِّح §٦ على هذا. **والتصحيحُ يُبلَّغ مديرَ
> المجموعة** ليطبّقه في نسخ العائلة (الوثيقة نسخةٌ مطابقة في المستودعات).

**والحدُّ الفاصل**: لا تجاوزَ للهوية على القواعد — لوحٌ جديد لا يغيّر قاعدةَ
تفاعل، واستعارةٌ جديدة لا تمسّ عهداً. الافتراقُ وجهٌ، والاشتراكُ عظم.


## ١٠. قراراتُ ١٥ أغسطس ٢٠٢٦ (المالك، بعد جلسة تخطيطٍ مطوّلة — والتصويبُ من مدير المجموعة)

### أ) بنيةُ العائلة النهائية — ١٣ وحدةً في ٧ تطبيقات
- **الجذع**: اقرأ واكتب **نسخةً واحدة لكلٍّ** — بوابتا دخولٍ لا مستويات (فكُّ الشيفرة مهارةٌ تُتقَن أو لا، واتجاهُ القلم كذلك).
- **اِتْقِنْ = العربيةُ المتقدمة** (لا الحفظ — تصويبُ تسميةٍ نهائي): ثلاثةُ مستويات — ١) الإملاء وقواعد الرسم؛ ٢) نحوٌ أساسي + فهمُ المقروء (يمتصّ «اقرأ ٢»)؛ ٣) نحوٌ متقدم + تعبيرٌ موجَّه. والتعبيرُ الحرّ والطلاقةُ الجهرية المقيسة **خارج النطاق عمداً** (يكسران «فاحصٌ آليّ محليّ»).
- **رَتِّلْ = التجويد** نسخةً واحدة متّسعة، و**اِحْفَظْ = حفظُ القرآن** (عضوٌ سابع جديد) نسخةً واحدة متّسعة. **ولا تمييزَ صوتٍ حيّ فيهما** — تمارينُ تمييزٍ واختيارٍ مرئية-سمعية، والتسجيلُ للطفل ووالده كعهدنا.
- **احسب والإنكليزية**: ثلاثةُ مستوياتٍ لكلٍّ.
- **معيارُ المستويات** (قاعدةً): يتغيّر **شكلُ التمرين** بين المراحل ⇒ مستوياتٌ منفصلة؛ تتغيّر **القاعدةُ المدرَّسة** وشكلُ التمرين ثابت ⇒ نسخةٌ واحدة متّسعة.
- **تصويبُ وهمٍ قُيِّد**: لا وجودَ لأي تطبيقٍ قرآنيٍّ مبنيّ — ما ظنّته جلسةُ التخطيط «احفظ المبنيَّ باسم اتقن» كان **بطاقةَ وصفٍ "قادم" في بوابة العائلة** قرأها نموذجٌ خارجيّ تطبيقاً حياً. لا مستخدمين ولا نطاقَ ولا تقدماً محفوظاً — والبطاقة صُوّبت في منبعها.

### أ-٢) تحديث ١٦ أغسطس ٢٠٢٦ (المالك، على توصية مدير المجموعة): سبعةُ تطبيقاتٍ فردية والمستوياتُ داخلية
- **البنية النهائية المعدَّلة**: التطبيقات **سبعةٌ لا غير** — اقرأ · اكتب · احسب · **اِسْمَعْ** · اتقن · رتل · احفظ — **لا «٢» ولا «٣» لأي تطبيق**: مستوياتُ اتقن الثلاثة واحسب واِسْمَعْ **مراحلُ داخلية في تطبيقٍ واحد** لكلٍّ، على نمط مفاصل اقرأ المسمّاة (٢٩٣ حلقةً في تطبيقٍ واحد أثبتته، والعلّةُ المقيَّدة «كتلةٌ بلا مفاصل لا عددٌ كثير»). وبوابةُ learn بسبع بطاقاتها كانت على هذه البنية سلفاً — فالمعلَنُ للناس لم يتغير، وإنما طابقه الداخل.
- **وحدةُ الإغلاق ملفُّ المستوى لا التطبيق**: كلُّ مستوىً يُفتح ببوابات العائلة كاملةً (دراسة ← خطة ← اعتماد ← تنفيذ ← مراجعة مستقلة ← ميدانُ طفل) ويُغلق ملفاً مستقلاً — **وبطاريةُ المستوى المغلق تبقى خضراء شرطاً** على كل جلسةٍ لاحقة، فإغلاقُ «احسب — المستوى الأول» قائمٌ لا يُمَسّ.
- **ثلاثةُ شروطٍ هندسية** تُكتب في خطة أول مستوىً ثانٍ يُفتح: بنكُ الصوت يُخزَّن **بقدر المستوى المفتوح** لا دفعةً (جهازُ ابن الرابعة لا يحمل بنك التاسعة)؛ **بوابةُ تحديد مستوى** للداخل المتأخر؛ ونبرةُ شاشات كل مستوىً تكبر مع صاحبها والعلامةُ واحدة.
- **معيارُ (أ) يبقى نافذاً** لرسم حدود المستويات (تغيّرُ شكل التمرين ⇒ مستوىً جديد؛ تبدّلُ القاعدة وحدها ⇒ اتساعٌ في المستوى نفسه) — وإنما تبدّل مسكنُ المستويات: مراحلُ داخل التطبيق لا تطبيقاتٌ شقيقة.

### ب) حدُّ المجموعة: ١٠–١٢ حلقة سقفاً
لا مجموعةَ في خريطة أي تطبيقٍ تتجاوز **١٢ عقدة** — وما زاد يُشقّ مجموعاتٍ أصغرَ مسمّاةً بنوعها، كي لا يتوه الطفل ويحسّ بالتقدم. (نظيرُ درس «مفاصل القرآنية» في اقرأ — صار قاعدةً عامة.) **وتوضيحُ المالك (١٥ أغسطس): الحدُّ سقفٌ لا هدف** — الأقلُّ من عشرٍ صحّيٌّ مقصود، وكثيرٌ من المجموعات الجيدة دونها؛ فلا تُنفخ مجموعةٌ صغيرة لتبلغ الحدّ.

### ج) المراجعةُ المنهجية المستقلة — شرطُ إغلاقٍ ثابت لكل تطبيق
قبل اعتبار أي تطبيقٍ «مكتملاً يُعمَّم»: **جلسةُ فابل مستقلة** (لا ميراثَ لقراراتنا عندها) تراجع منهجيتَه العلمية على وثيقة تكليفٍ يكتبها مديرُ المجموعة — ترتيبَ الرحلة، وصحةَ التدرج مقابل الأدبيات، وأحمالَ الطفل — وتُرتَّب ملاحظاتُها بالخطورة وتُحسَم بقرار المالك. تسري على القائم (اقرأ أولاً، ثم احسب واكتب) وعلى كل قادم.

### د) «بلِّغنا» — نمطُ العائلة
بابُ بلاغٍ واقتراحٍ في **لوحة وليّ الأمر وحدها** (لا زرَّ في شاشة طفل): رابطا واتساب (**+97433882806**) وبريدِ العائلة (info@mishkat.qa) مملوءين بسطر سياقٍ (التطبيق وقشرته)، **ولا يُرفَق من بيانات الطفل شيء**. **وقاعدةُ بناءٍ من ميدان اقرأ**: الوحدةُ تُفصَل عن أي ملفٍّ يمرّ به صوتُ الطفل أو قلمُه — حارسُ الخصوصية يمنع `https://` في تلك الأسطح، والفصلُ حصانةٌ بنيوية (اقرأ: `feedback.js` والحارس `test_feedback.mjs` نموذجاً).


### هـ) بوابةُ اللحاق — امتحانُ تحديد المستوى الاختياري (قرار المالك، ١٦ أغسطس ٢٠٢٦)
جمهورُ العائلة ليس المبتدئَ من الصفر وحده: تلميذُ المدرسة والمراكز يصل بمستوىً قائم، وإجبارُه على أول السلّم مللٌ فهجر. فلكل تطبيقٍ (بجدول أولويته أدناه) **امتحانُ لحاقٍ اختياري** بخمسة قيودِ صدقٍ لا تُنقص:
1. **اختياريٌّ ومن لوحة وليّ الأمر حصراً** — لا زرَّ في شاشة طفل.
2. **يفتح ما أُثبت لا ما ادُّعي**: سلّميٌّ يصعد مجموعةً مجموعة ويقف عند أول شرخ.
3. **صرامتُه صرامةُ بوابات الإتقان** (≥٨٠٪) — وبواباتُ الإتقان نفسُها لا تُقفز: يوصِل إليها وتُجتاز بنفسها.
4. **نتائجُه تُزرع في ليتنر قياساً حقيقياً** — والمراجعةُ اليومية شبكةُ أمانه إن سخا الفتح.
5. **فتحٌ لا قفل**: لا يُغلق مفتوحٌ أبداً، ويُعاد متى شاء الوالد فيستأنف من آخر حدّ.

**النطاق والأولوية (لفظ المالك)**: «عملياً الآن **اقرأ واكتب جاهزان** للميزة، الباقون لاحقاً» — فاقرأ نافذٌ اليوم (حزمة «اللحاق» في سجل جلساته)، واكتب يليه **بامتحان يدٍ لا اختيار** (ادّعاءُ اليد لا يُقبل إلا من اليد)؛ و**احسب واِسْمَعْ واتقن مع مستوياتها القادمة** (تدخل خطةَ كل مستوىً يومَ يُفتح). أما **رتل واحفظ فقسمٌ قرآنيٌّ له اعتباراتُه الخاصة خارج هذه القاعدة** — يُقرَّر شأنُ كلٍّ في خطته يومَ يُبنى (وملمحُ احفظ الطبيعي «إثباتُ المحفوظ» تسميعاً اختيارياً — يُقيَّد في تكليف دراسته لا هنا).