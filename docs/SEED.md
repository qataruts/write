# بذرةُ «اُكْتُبْ» — جردُ ما نُسخ من «اِقْرَأْ»، ومن أيّ التزام، وبأيّ تعديل

> **العهد** (`FAMILY.md §٤`): «البذرةُ تُنسخ ولا تُستورَد — ويُقيَّد في `docs/SEED.md`
> **ما نُسخ ومن أيّ التزام**، فالاستخلاصُ اللاحق يعرف ما تفرّع». وهذا الملفُّ هو ذلك
> القيد. **ولا مكتبةَ مشتركة الآن**: الدراسةُ تنصّ على استخلاص المحرّك **بعد نضوج
> تطبيقين على الأقل**، وتَعُدّ التجريدَ المبكر خطراً.

| | |
|---|---|
| **المصدر** | `qataruts/read` — تطبيق «اِقْرَأْ» |
| **الالتزام** | `ea07f350e48475b111b62d8801f59dec2ac5f5a0` · ٨ أغسطس ٢٠٢٦ · «update email» |
| **تاريخ النسخ** | ١١ أغسطس ٢٠٢٦ — الجلسة ٠ |
| **قائمةُ ما يُنسخ** | `FAMILY.md §٥` حرفياً |
| **البصمة** | أولُ ١٢ خانة من `sha256` **ملفِّ المصدر** عند ذلك الالتزام — فيُعرَف يوماً أيُّ نسخةٍ تفرّعنا عنها |

**وكيف يُقرأ عمودُ «الحال»:**

- **حرفياً** — بايتٌ ببايت كما هو في اقرأ. لا فرقَ يُبحَث عنه.
- **مُعدَّل** — نُسخ ثم عُدِّل، والتعديلُ **معلَنٌ بسببه** في العمود الأخير.
- **لم يُنقَل** — قُصد تركُه، وسببُه في §٤. وصنفان لا ثالثَ لهما:
  · **حذفٌ** لِما لا موضوعَ له في «اُكْتُبْ» **أبداً** (قصصٌ · مصحفٌ · صوتُ طفل)؛
  · **تعليقٌ** لِما لم يحن بعدُ، **وموضعُه محفوظٌ بتعليقٍ مكتوب في الشيفرة نفسِها**
    لا في هذا الملف وحدَه — «ما لا موضوعَ له بعدُ يُعلَّق بإعفاءٍ مكتوبٍ بسببه لا يُحذف».

---

## ١. وحدات `app/js/` — المحايدةُ للمادة

| الملف | الحال | بصمةُ المصدر | التعديل وعلّته |
|---|---|---|---|
| `audio.js` | مُعدَّل ⟵ **كان حرفياً** | `abdf68d090c7` | — لا يعرف مادّةً: sha1 خالص، وبصماتُ المحتوى، واحتياطٌ مضبوط لغياب الملف لا لبطء الشبكة. · **🔴 وكانت النسخةُ الحرفية نسخةَ المعطوبة**: عيبُ تكدّس الأصوات (بلاغُ طفلةٍ حقيقية في اقرأ) كان عندنا بعينه — **نُقل إصلاحُه بحارسه في الجلسة ٤ب، §٧ أدناه**، والبصمةُ الجديدة `081aca0ada57` عند `9220ab1`. |
| `progress.js` | مُعدَّل | `badd8869c473` | **الرحلةُ تُبنى من `STAGES` لا من مجموعات اقرأ**: سقطت `groupNodes`/`quranSections`/`gardenNodes`/`ladderNodes`/`libraryNodes`/`shelfNodes`/`rootNodes`/`rootPlacement` وحلّت محلَّها `stageNodes` واحدة. · **محاورُ ليتنر باسم اكتب**: `(حرف × حركة)` ← `(وحدة × شكلُ موقع)` بنصّ `METHOD §٦` — بدَّلَ الاسمَ لا البنية، فلا يُعاد تسميةُ محورٍ مرتين في الجلسة ٥. · `KINDS` صارت الأربعةَ المنصوصةَ في `METHOD §٦`. · مفتاحُ التخزين `uktub.progress.v1` و`BACKUP_KIND` تبعه. · **سجلُّ «اقرأ لي» لم يُنقَل** (§٤). · باقي الوحدة كما هي: النجومُ والجبهةُ وليتنر والنسخةُ الاحتياطية والمعاينةُ وصلابةُ التخزين. |
| `ui.js` | مُعدَّل | `e13fba87f47e` | `BRAND` = «اُكْتُبْ»، و`brandMark()` **بلا الطائر** بعلّةٍ مكتوبة في موضعها (ضمّةُ الألف فوقها، فيحجبها الطائر) — و`mascot()` باقيةٌ يستعملها الاحتفال. · `nodeTitle`/`nodeWhere`/`nodeFace` على أنواع محطات `METHOD §٤`. · `accentForKind` مكانَ ألوان اقرأ. · زِيدت أيقونتان خطيّتان: `pen` و`link`. · `coverEl` و`micIcon` **لم تُنقَلا** (§٤). · باقي الوحدة كما هي: `h` وأيقوناتُ الواجهة و`faceEl` و`DEV` و`giantInk` و`heroStep` و`landmark` و`shuffle`. |
| `review.js` | مُعدَّل | `2e3c073be823` | **هيكلُه لا تمارينُه** (نصُّ `FAMILY §٥`): بقي `renderSession` بعدّاد خطواته وتسجيلِ المحاولة وسلوكِ الخطأ والصواب وسؤالِ المغادرة وشاشةِ الختام، و`starsForReview`، و«مراجعةُ اليوم». **وتمارينُ اقرأ الثمانية لم تُنقَل**: سماعيةٌ وقرائية، ومادّةُ هذا التطبيق حركةُ قلم. وحلّ محلَّها سجلٌّ `VIEWS` **فارغٌ مُعلَّق** تملؤه الجلسة ٥. |
| `gate.js` | مُعدَّل | `883c2dc6004b` | القواعدُ الأربع كما هي (الأضعفُ مادّةً · لا محتوى جديد · لا رسوب · الحكمُ بالمحاولة). سقط `studiedPairs` (خاصُّ اقرأ)، وصار السطرُ «قَوِّ يَدَكْ أَوَّلاً» مكان «قَوِّ حُرُوفَكْ». |
| `parent.js` | مُعدَّل | `c1f681417669` | **البنيةُ والبوابةُ الحسابية** (نصُّ `FAMILY §٥`): بقيت البوابةُ والقشرةُ وأقسامُ الزمن والتوصيةِ والنسخةِ الاحتياطية والتحكّمِ في الرحلة والمعاينة. **وأقسامُ مادة اقرأ لم تُنقَل**: العلاماتُ والجذورُ و«نحو القراءة الحرة» و«تسجيلات طفلي» (§٤)، وحلّ محلَّ الأخيرين قسمُ **«الكلمات نسخاً وإملاءً»**. **والقسمُ الذي تنتظره هذه اللوحة عدّادُ أخطاء الاتجاه المميَّز** (`METHOD §٦`) — تملكه الجلسة ١٠. |
| `main.js` | مُعدَّل | `d2b1b608fc1e` | **الهيكل** (نصُّ `FAMILY §٥`): الخريطةُ الكسولة بأدراجها و`FOLD_NEAR`، وخيطُ الدرب، وبطاقةُ «تابع من هنا»، وبطاقةُ المراجعة، والقفلُ في الموجّه، وساعةُ الاستخدام، وتسجيلُ عامل الخدمة، وشريطُ المعاينة، وبابُ التعريف في الذيل. · **مُصيِّرٌ واحد للمحطات** (`stationEl`) مكان تسعةٍ في اقرأ — يقرأ عنوانَه من بيان محطته. · **جردُ الشاشات المنتظَرة مكتوبٌ لا مضمَر** (`SCREENS`): لكل نوعٍ سطرٌ يقول مَن يملك شاشتَه، فلا يقع الطفلُ على طريقٍ مسدود. · شاشاتُ اقرأ لم تُنقَل. · **وزِيد في الجلسة ٤ب** (§٧): تركيبُ شريط التثبيت، وإعادةُ إعلان الميتا عند العودة من الخلفية. |
| `curriculum.js` | **لم يُنسَخ** | — | نصُّ `FAMILY §٥`: «لا يُنسخ `curriculum.js` وكلُّ بياناتِ ومحتوى وشاشات المادة». وكُتب هنا **هيكلٌ فارغٌ يُعلن الشكل ولا يحمل مادة** — تملؤه **الجلسة ٣** بأداة اشتقاقٍ تقرأ بيانات اقرأ آلياً. وعلّةُ كتابته فارغاً لا تركِه غائباً: العهدُ «خريطةٌ فارغة تفتح بلا أخطاء»، والوحداتُ كلُّها تقرأ منه. |

## ٢. قشرة التطبيق و`sw.js` واللوح

| الملف | الحال | بصمةُ المصدر | التعديل وعلّته |
|---|---|---|---|
| `app/sw.js` | مُعدَّل | `2204dc56bbf2` | سلوكُ `v20` كاملاً (مخزنُ صوتٍ ثابتُ الاسم، جلبُ الناقص وحدَه، دفعاتُ ١٦، إخفاقٌ معدودٌ لا مبتلَع، شفاءٌ عند أول اتصال، شفافيةُ تحميل) — **ونسخةُ اكتب تبدأ من `v1`**: البذرةُ تُنسخ ولا تُستورَد، وتاريخُ نسخِ اقرأ تاريخُه هو. · `SHELL` ملفاتُ اكتب، وأسماءُ المخازن `uktub-*`. · **ولا يُكتب فهرسٌ لبنكٍ لم يُنشأ**: `audio/manifest.json` و`emoji/index.json` خارج `SHELL` اليوم، **ويُطالِب بهما `test_pwa.mjs` من نفسه** يومَ يظهران على القرص. · `precacheStories` **لم تُنقَل** (§٤)، و`precacheEmoji` باقيةٌ تعمل بلا فهرسٍ بلا ضرر. |
| `app/css/app.css` | مُعدَّل | `c92471a80ed2` | **🎨 واللوحُ اللونيُّ ما عاد لوحَ اقرأ** (الجلسة هـ١، ١٢ أغسطس ٢٠٢٦): حكمُ المالك `indigo / shift` صبغ الرموزَ الخمسةَ عشرَ (المراحلُ الخمس والعلامةُ الثلاثيةُ والأرضيةُ السبعة) ومعها كتلةُ الليل مشتقّةً — **فما بقي من البذرة هنا هيكلُها لا وجهُها** (`FAMILY §٩`)، وألوانُ الحكم وحدَها (`--star`/`--ok`/`--err`) باقيةٌ بأعيانها عهداً لا إرثاً. ويحرسه `identity_panel.py --self-test`. · وأصلاً: القشرةُ والشريطُ العلويّ والقشرةُ والشريطُ العلويّ والخريطةُ ودرْبُها ومعالمُها وشريطُ الخطوات والاحتفالُ وترويسةُ البوابة والرسالةُ العابرة وأدواتُ `?dev=1` وأقسامُ لوحة الوالد والوضعُ العرضيّ ولغتا الرموز. · **خمسةَ عشرَ قسماً من شاشات اقرأ لم تُنقَل** (§٤). · أصنافُ المحطات صارت أنواعَ `METHOD §٤`. · **زيادةٌ معلَنة**: `.btn--ghost` عرضٌ أدنى ٣rem — أمسك `browser_test.py` يومَ كُتب زرَّ لوحة الوالد عند ٤٢ بكسلاً عرضاً و`DESIGN §٤` يشترط ≥٤٨. **والعيبُ قائمٌ في اقرأ نفسِه فيُعاد إليه** (عادةُ العائلة: ما تُصلحه فرعٌ يعود إلى أصله). · **🔴 نقصٌ في النقل نفسِه كُشف في الجلسة ٣ وأُصلح**: قاعدةُ `.dot--done` فقدت سطرَيها وقوسَها الخاتم أثناء التشذيب، **فابتلعت ما بعدها إلى السطر ١١٢١** — نحو ثلاثمئة سطرٍ ماتت صامتةً (أيقوناتُ الواجهة `.ui-icon` ووجوهُ العقد وقياساتُ الشاشات الصغيرة والوضعُ العرضيّ والاحتفالُ وأقسامُ لوحة الوالد). ولم يظهر حتى امتلأت الخريطةُ بعقدها اليوم: **أمسكته العينُ في لقطة** (وجوهُ العقد فارغة)، ثم عدُّ الأقواس. **وليس عيباً في اقرأ** — عنده القاعدةُ تامّة (`--ok`)، فهو خطأُ نسخٍ عندنا رُدّ إلى أصله. **وشقُّه الثاني** (شرطُ إتمام مراجعة المدير): القوسُ المنزوعُ من موضعه بقي **يتيماً بعد ثلاثمئة سطر** (١١٢٤)، فكان الملفُّ متوازناً عدداً وهو معطوبٌ اقتراناً — نُزع. **وأُغلق الصنفُ بحارس**: `test_pwa.mjs §٥` يجرد ألواحَ CSS من القرص ويحكم بقاعدة CSS نفسِها («القاعدةُ لا تلد قاعدة»)، مجرَّباً سالباً بأربع حالات. · **وزِيد في الجلسة ٤ب** (§٧): `touch-action: manipulation` على `html`، وكتلةُ `.install-bar`. · **🔴 وعيبُ بذرةٍ رابعٌ كُشف في الجلسة م١ ويُعاد إلى اقرأ**: `.toast` **بلا `pointer-events: none`** — شريطٌ مثبَّتٌ في ذيل المنظور بـ`z-index: 20` **يبتلع كلَّ نقرةٍ تحته ٢٫٢ ثانية**. وقيس في مشهد بلاغ الميدان بعينه على مقاس آيباد طولي: طفلٌ ينقر محطةً مقفلةً فتظهر الرسالة، ثم يمدّ يدَه إلى عقدةٍ أخرى **في ذيل المنظور فلا تستجيب** — قيست عقدةُ «حرف د» تحتها بعينها (`⇐ div.toast`) — حتى إذا سحب الشاشةَ مضت المهلةُ فذهبت الرسالةُ فعملت النقرة، وهو نصُّ البلاغ. **وأيُّ عقدةٍ تقع في ذيل المنظور تُصيبها العلّةُ نفسُها**، والخريطةُ تُسحَب فتتبدّل مَن تحتها. **والكتلةُ في اقرأ نفسُها بلا الخاصّية** (`read/app/css/app.css:1791`) فيُعاد إليه — رابعُ ما تردّه هذه الشجرة إلى أصلها بعد `.btn--ghost` وتشديدِ `/result` و`.install-x`. |
| `app/index.html` | مُعدَّل | `d3f815256ff5` | القشرةُ نفسُها بنصوص «اُكْتُبْ» وعنوانِه ووصفِه. · **وزِيد في الجلسة ٤ب** (§٧): قفلُ المقياس في الميتا. |
| `app/manifest.webmanifest` | مُعدَّل | `be90f7838e88` | البيانُ نفسُه بهوية «اُكْتُبْ» (والاسمُ القصير **بلا شكل** — استثناءُ `DESIGN §٦` المعلَن). |
| `app/fonts/` (٥ ملفات) | **حرفياً** | Marhey `dc16cff1ad9c` · NotoNaskh `bb07eddbc50a`/`55cc6294c78b` · Baloo `ddfc8d7ce4a7`/`9716a70e65ce` | **وخطُّ المصحف `KFGQPCUthmanicHafs.woff2` لم يُنقَل**: لا نصَّ مصحف في «اُكْتُبْ». |
| `app/icons/` (٤ ملفات) | **مولَّدة** | — | ليست منسوخة: ولّدها `tools/make_icons.py` من `tools/icon.html` بعلامة «اُكْتُبْ». **وألوانُها ألوانُ البذرة** حتى تحسمها الجلسة ١١ مع صفحات welcome. |

## ٣. عدّة `tools/` و`docs/`

| الملف | الحال | بصمةُ المصدر | التعديل وعلّته |
|---|---|---|---|
| `test_selftests.mjs` | **حرفياً** | `d0ac7dc560c2` | حارسُ الحرّاس: يجرد `tools/` عن كل ما يُعلن `--self-test` ويشغّله. لا يعرف مادّةً، فما وُجد بالجرد دخل السَّوقة. |
| `test_pwa.mjs` | مُعدَّل | `af3a7b4f2b9f` | سقطت مطالبُ بياناتِ اقرأ (تلاوةٌ وقصص). و**فحوصُ البنك الصوتيّ مُعلَّقةٌ بشرطِ وجود الفهرس على القرص** — فتصير حمراءَ يومَ يُصرَّف أوّلُ صوت بلا سطرٍ يُعدَّل. وجردُ `SHELL` من القرص كما هو. · **وزِيد في الجلسة ٣**: §٥ حارسُ أقواس الألواح. · **وزِيد في الجلسة ٤ب** (§٧): §٦ قفلُ المقياس. |
| `test_measure.mjs` | مُعدَّل | `97d952b78dc3` | **جردُ محطات اقرأ لم يُنقَل**، وفيه اليومَ سطرُ البوابة وحدَه (وهي قائمة). **وجردُ اكتب تملؤه الجلسة ٣** بنصّ بندها. والتعليقُ يُطالِب من نفسه: أوّلُ محطةٍ تدخل الرحلةَ بلا سطرٍ تُسقِطه. |
| `test_nodes.mjs` | مُعدَّل | `5ed766515388` | العهدُ نفسُه («لا عقدةَ بلا كاتبِ نجمة») على رحلةٍ فارغة: يُعلن التعليقَ ولا يدّعي خُضرة، ويعمل يومَ تدخلها أوّلُ عقدة. وزِيد فحصٌ نصيّ يثبت أنّ البوابةَ والرحلةَ تشتقّان معرّفَها من مصدرٍ واحد — وهو عينُ العيب الذي وُلد منه هذا الحارس، مسدوداً بالبناء. |
| `test_audio_cache.mjs` | مُعدَّل | `c84217e046e8` | الشطرُ الأول (بصماتُ الشجرة) مُعلَّقٌ حتى يُنشأ البنك. **والشطرُ الثالث يعمل من اليوم**: يشغّل `app/sw.js` نفسَه على كاشٍ وشبكةٍ مزيَّفين — وهو الشاهدُ على سلامة السلوك قبل أن يوجد ما يُخزَّن. سقطت أعيانُ التلاوة والقصص من بيئته المزيَّفة. |
| `browser_test.py` | مُعدَّل | `6ec472d34301` | **الحظيرةُ لا السَّوقة**: الخادمُ ومُشغِّلُ Chrome واستقبالُ النتيجة **وعدّادُ طلبات الصوت عند الخادم** (يحتاجه فحصُ خصوصية القلم في الجلسة ١). سقطت صفحاتُ اقرأ الاثنتان والعشرون وراياتُها، و`device_main` **مُعلَّقةٌ للجلسة ١٢** و`DEVICE_SIZES` باقيةٌ لها. · **زِيد في الجلسة ١**: سَوقةُ صفحاتٍ (`--suite`) وصفحةُ `browser_pen.html`، و**تشديدُ `do_POST` على `/result` وحدَه** — كان أيُّ POST يُقرأ نتيجةً، فأمسكه فحصُ خصوصية القلم يومَ جُرِّب سالباً: الرفعُ المزروع في `pen.js` حلّت حمولتُه محلَّ النتائج وانهار العدّاء بدل أن يحمرّ. **والعيبُ قائمٌ في اقرأ نفسِه فيُعاد إليه** (كنظير `.btn--ghost`). · **وزِيد في الجلسة ٢**: صفحةُ `browser_paths.html` في السَّوقة (`--suite paths`) — ولا تعديلَ في الحظيرة نفسِها، بل **استعارةٌ معلَنة**: `tools/make_paths.py` يستورد خادمَها ومُشغِّلَ Chrome ويسجّل صفحتَه في `PAGES` (نظيرُ استيراد `trim_lead.py` لـ`audio_audit.py`) — فخادمٌ ثانٍ لا يزيد إلا موضعَ عطبٍ ثانياً. · **وزِيد في الجلسة ٤**: صفحةُ `browser_warmup.html` في السَّوقة (`--suite warmup`) و`warmup_parts()` تقرأ محطاتِ التهيئة من وحدتها المولَّدة (لا قائمةَ تُكتب) — زيادةٌ على السَّوقة لا على الحظيرة. |
| `browser_test.html` | مُعدَّل | `1530bdfb03c9` | كُتبت من جديد: **فحصُ القشرة على تطبيقٍ فارغ** — معيارُ قبول هذه الجلسة نفسُه (يفتح بلا خطأ جافاسكربت)، ومعه الخريطةُ والعلامةُ وهدفُ اللمس واللاتمريرُ الأفقيّ وبوابةُ لوحة الوالد و**صفرُ مصدرٍ خارجيّ**. |
| `check_live.py` | مُعدَّل | `b0c39f2c128e` | النطاقُ `write.mishkat.qa`. **وسيرُ النشر مُعلَّقٌ بشرطِ وجود `pages.yml`** — الجلسة ١١ تنشئه، ويُفحَص يومَ يظهر. وقائمةُ الصفحات مشتقّةٌ من الشجرة كما هي. |
| `cdn_check.py` | مُعدَّل | `91acc3855dcb` | النطاقُ `write.mishkat.qa`، وسقط بيانُ التلاوة. **وعيّنةُ القشرة صارت مشتقّةً من الشجرة** (ما وُجد فُحص) بدل قائمةٍ مكتوبة — فلا يُطلَب من الشبكة ملفٌّ لم يُنشأ فيُرَدّ ٤٠٤ فيبدو عيباً وليس بعيب. وقاعدةُ `immutable-assets` تسري على النطاق الجديد بلا إعدادٍ إضافيّ (`FAMILY §٦`). |
| `perf_check.py` | **حرفياً** | `5583c03f7b0b` | تشغّل `app/welcome/perf-check.html` وتجمع أرقامَها نصّاً — لا تعرف مادّةً. |
| `generate_audio.py` | **حرفياً** | `6fb32ccfd726` | لم يُمَسّ بحرف — **تملكه جلسةُ الصوتيات وحدها**. يقرأ `app/js/curriculum.js` (فارغٌ اليوم)، فلا يستخرج شيئاً حتى يُملأ. |
| `audio_panel.py` | **حرفياً** | `1dfabe633e8d` | لوحةُ المراجعة **بالأذن** — شرطُ اعتماد أيّ ملف. |
| `trim_lead.py` | **حرفياً** | `8f6abd33bf4d` | مِقصُّ الصمت الأول، بفحصه الذاتي (١١ تحقّقاً، يعمل بلا ملفات). |
| `audio_audit.py` | **حرفياً** | `c88a83d2fea6` | **ليس من قائمة `FAMILY §٥`** — نُسخ لأنّ `trim_lead.py` يستورده، ولا يعمل بدونه. **تبعيةُ أداةِ بذرةٍ مُعلَنة، لا زيادةٌ على القائمة.** |
| `make_icons.py` + `icon.html` | مُعدَّل | `33f3b430bf3e` · `bace774f87fb` | كلمةُ العلامة «اُكْتُبْ»، ووسمُ المجلد المؤقّت. والقياسُ ورفعةُ الحبر كما هي. |
| `fetch_twemoji.py` | **حرفياً** | `9ad60b05e0d6` | يجرد المستعمَل من مصادر المنهج فيُنزِّله — يعمل يومَ يكون في المنهج رمز. |
| `docs/AUDIO_QUEUE.md` | **قالبٌ مُعاد تأليفه** | — | البروتوكولُ ملزمٌ كما هو (لا تشغّل المولّد · لا تلمس `app/audio/` · مشكولاً حرفياً · تقريرُ الجلسة). **وسجلُّ اقرأ لم يُنقَل**: ماضيه ماضيه. وزِيد ما يخصّ اكتب: **أصواتُ اقرأ تُنسخ ببصماتها ولا تُولَّد**، وما لا يدخل القائمة أبداً. |
| `docs/DESIGN.md` | **قالبٌ مُعاد تأليفه** | — | اللوحُ وأهدافُ اللمس والوضعان وتوسيطُ الحبر ولغتا الرموز وقوانينُ حدس الطفل كما هي. **وحُذف ما لا موضوعَ له** (صدقُ الصورة بدرجاته الثلاث: أحواضُ أسئلةٍ مصوَّرة لا وجود لها هنا)، **وزِيد §٨ «ما يخصّ الكتابةَ وحدَها»**. |
| `app/welcome/` | بنيةٌ فقط | `welcome.css` **حرفياً** `e3fe4e46ba1a` · الخطوطُ الستّة حرفياً | نصُّ `FAMILY §٥`: «**بنيةُ** `welcome/` الأربع». فالصفحاتُ الأربع قشرةٌ وشريطُ تنقّلٍ وعنوان — **ولا حقيقةَ تُدَّعى عن تطبيقٍ لم يُبنَ**: صفحةُ عرضٍ تصف ما ليس قائماً كذبٌ على معلّم. **تملؤها الجلسة ١١.** وقوانينُ `welcome/` قائمةٌ من اليوم: صفرُ جافاسكربت · خارج قشرة عامل الخدمة · صفرُ مَوردٍ خارجيّ · لوحُ `app.css` نفسُه. و`perf-check.html` مُعدَّلةٌ (`83092294c655`): أسماءُ المخازن وأصنافُ الشاشات. |

## ٤. ما لم يُنقَل — ولكلٍّ سببُه

### أ) حذفٌ: لا موضوعَ له في «اُكْتُبْ» **أبداً**

| ما هو | العلّة |
|---|---|
| `curriculum.js` وكلُّ بيانات المادة (`lexicon.json` · `stories/` · `recitations.json` · `emoji/`) | نصُّ `FAMILY §٥`. ومنهجُ اكتب يُشتقّ من بيانات اقرأ آلياً في الجلسة ٣. |
| شاشاتُ اقرأ (`lesson` · `words` · `skill` · `story` · `quran` · `garden` · `ladder` · `library` · `contrast` · `roots` · `sentences` · `screens` · `lexicon`) وتنسيقاتُها في اللوح | مادّةُ هذا التطبيق شاشاتُه هو. |
| `recorder.js` · `recordings.js` · `record.js` وسجلُّ «اقرأ لي» في `progress.js` و`parent.js` وأيقونةُ الميكروفون | **«اُكْتُبْ» لا يلتقط صوتاً ألبتّة**. وأثرُ الطفل الخاصّ هنا **مساراتُ قلمه**، وعهدُها أشدّ: لا تغادر جهازه، و`pen.js` لا يعرف الشبكة بنيوياً (`METHOD §٣.٧`) — يحرسه فحصان في الجلسة ١. |
| `recitation.js` وخطُّ المصحف وكلُّ ما يخصّ نصَّ المصحف | لا نصَّ مصحف هنا (`METHOD §١٠`). |
| `coverEl` وأغلفةُ القصص و`precacheStories` | لا قصصَ في «اُكْتُبْ» (`METHOD §٤`: مراحلُه أربعَ عشرة، ليس فيها قراءةٌ مؤلَّفة). |
| `markStats` وقسمُ العلامات في اللوحة | علاماتُ اكتب (الشدّةُ والتنوينُ والهمزات) **ليست دروساً مستقلّة**: أقرّ المالك (ق٣) أن تُكتب **في مواضعها ضمن الكلمات**، فلا مفتاحَ ليتنر لها. |
| `fade.js` و«نحو القراءة الحرة» | خفوتُ اكتب يخفي **النموذجَ المكتوب** لا شكلَ الكلمة، وعدّادُه قائمٌ في `progress.js` من اليوم — والشاشةُ للجلسة ٩. |

### ب) تعليقٌ: لم يحن بعدُ — وموضعُه محفوظٌ بتعليقٍ مكتوبٍ في الشيفرة

| ما هو | مَن يملكه |
|---|---|
| تمارينُ المراجعة (`VIEWS` في `review.js`) | الجلسة ٥ |
| جردُ `test_measure.mjs` و`test_nodes.mjs` على محطات اكتب | الجلسة ٣ (بنصّ بندها) |
| فحوصُ البنك الصوتيّ في `test_pwa.mjs` و`test_audio_cache.mjs` | جلسةُ الصوتيات (والفحصُ يُطالِب من نفسه يومَ يظهر الفهرس) |
| سيرُ النشر في `check_live.py`، وعيّنةُ الصوت في `cdn_check.py` | الجلسة ١١ · جلسةُ الصوتيات |
| عدّةُ الأجهزة (`device_main` + `browser_device.html`) | الجلسة ١٢ |
| محتوى صفحات `welcome/` الأربع | الجلسة ١١ |
| طائرُ العلامة وألوانُ الأيقونة | الجلسة ١١ |
| عدّادُ أخطاء الاتجاه المميَّز في لوحة الوالد | الجلسة ١٠ (وأصلُه في `pen.js`، الجلسة ١) |

## ٥. ما يُنسخ لاحقاً من اقرأ — وليس بذرةَ منصّة

هذه **أصولُ مادة** لا شيفرة، ويقيَّد نسبُها هنا يومَ تُنسخ (`METHOD §٧` و`§٨`):

| ما هو | الجلسة | القيد المطلوب |
|---|---|---|
| ترتيبُ المجموعات السبع وحروفُها | ٣ ☑ | تُقرأ آلياً، ولا تُنسخ ملفاتٍ — §٦ أدناه |
| بنكُ الكلمات المصوَّرة وسلّمُ الجمل | ٣ ☑ · ٨ · ٩ | تُقرأ آلياً — §٦ أدناه |
| أصواتُ أسماء الحروف | ٥ | ملفاتٌ **بأعيانها ببصماتها** ← يُقيَّد كلُّ ملفٍ هنا |
| أصواتُ كلمات الإملاء وجملِه | ٩ | ملفاتٌ **بأعيانها ببصماتها** ← يُقيَّد كلُّ ملفٍ هنا |

> **ولماذا البصمة شرط؟** لأنّ اسمَ ملف الصوت من **نصّه** لا من محتواه. فلو نُسخ ملفٌّ
> ثم تغيّر أصلُه في اقرأ لَسُمِعت الكلمةُ الواحدة بصوتين — وهو عينُ العيب الذي وُضع
> نظامُ البصمات في اقرأ لمنعه. فالقيدُ هنا يجعل الفرقَ **مرئياً** يومَ يقع.

## ٦. المنهجُ المشتقّ (الجلسة ٣) — **لا ملفَّ نُسخ، وبيانٌ يُقرأ**

`app/js/curriculum.js` **مولَّد**: يكتبه `tools/make_curriculum.mjs` باستيراد وحدات
اقرأ نفسِها وقتَ التوليد — **فلا سطرَ منقول ولا كلمةَ منسوخةٌ بيد**، والنسبُ في
`CURRICULUM_SOURCE` داخل الوحدة نفسِها. وهذه المصادرُ الأربعة ببصماتها:

| ملفُّ اقرأ | البصمة | ما أُخذ منه |
|---|---|---|
| `app/js/curriculum.js` | `f0cd96955c3b` | `LETTERS` (٢٨ حرفاً بأسمائها ووصلِها) · `GROUPS` (المجموعاتُ السبع بترتيبها وحروفِها وكلماتِها) · `SKILLS` (كلماتُ الشدّة والتنوين ولام التعريف — حاملاتُ علامات ق٣) · `HARAKAT` و`SUKUN` (أسماءُ العلامات) · `letterForms` (وجهُ عقدة الشكل) |
| `app/js/sentences.js` | `a2aff2084a59` | `SENTENCES` (سلّمُ الجمل بترتيبه) و`stemOf` (تجريدُ الكلمة من «الْ» وحركة الإعراب) |
| `app/data/lexicon.json` | `fa3b5e9f4903` | `words` (معجمُ حديقة الكلمات) · `support` (كلماتُ الجمل المساندة) · `bundleSize` (**حِمْلُ المحطة الواحدة — رقمُ اقرأ لا رقمُنا**) |
| `app/audio/manifest.json` | `6d0a0498afcc` | مفاتيحُ بنك الصوت: **أللكلمةِ صوتٌ عنده؟** — وما لا صوتَ له يُنسَخ ولا يُملى |

**والمأخوذُ منها في المنهج**: ٢٨ حرفاً · ٨٠ كلمة · ٢٠ جملة · ١٠ علامات — كلُّها
تُقرأ عند كل توليد، فتغيُّرُ بيانات اقرأ يظهر في `--self-test` (المصدرُ يُبصَم
والوحدةُ تُقابَل بما يولّده المولّد بايتاً ببايت).

**ولا صوتَ نُسخ بعد**: هذه الجلسةُ تقرأ الفهرسَ لتعرف ما له صوت، **ولا تنقل ملفاً**
— نقلُ الملفات ببصماتها للجلستين ٥ و٩ كما في الجدول أعلاه.

**وما أُعلن هنا ولم يُشتقّ** (وهو كلُّ ما في الوحدة من غير مادة اقرأ): محطاتُ
التهيئة الست بنصّ `METHOD §٤` (ليس عند اقرأ ما يوازيها)، و`VARIANTS` — الحروفُ
المتغيّرة (أ إ آ ؤ ئ ء ة ى) بأصولها: **القراءةُ لا تفرّقها والرسمُ يفرّقها**، فليس
عند اقرأ بيانُها.

## ٧. منقولاتُ اقرأ الميدانية (الجلسة ٤ب) — **نقلٌ بعد البذرة، والتزامٌ غيرُ التزامها**

أمرُ المالك (١١ أغسطس ٢٠٢٦): ثلاثةُ منقولات **طُبّقت في اقرأ واختُبرت ميدانياً** —
التثبيتُ جرّبه المالك وأقرّه، وعيبا الصوت والزوم **بلاغا طفلةٍ حقيقية**. فتُنقل
بحرّاسها. **وهذه أوّلُ زيادةٍ على البذرة من التزامٍ غير `ea07f35`**، فلها التزاماها:

| | |
|---|---|
| **التزام باب التثبيت** | `260bf48fc4c06c8d41406a386f3e4fdfe25c8bb4` · ١١ أغسطس ٢٠٢٦ · «بابُ التثبيت: شريطٌ يقول لكل جهازٍ ما يخصّه، ويصمت في التطبيق المثبَّت» |
| **التزام بلاغَي الابنة** | `9220ab1ce0f18461e2661f1467294046e9bdfe8d` · ١١ أغسطس ٢٠٢٦ · «بلاغا الابنة: لا تكدّسَ للأصوات بالنقر السريع، ولا زومَ عند العودة من الخلفية» |
| **تاريخ النقل** | ١١ أغسطس ٢٠٢٦ — الجلسة ٤ب |

| الملف | الحال | بصمةُ المصدر | التعديل وعلّته |
|---|---|---|---|
| `app/js/audio.js` | مُعدَّل | `081aca0ada57` (عند `9220ab1`) | **إصلاحُ التكدّس بعينه**: جيلُ تشغيلٍ (`epoch`) يزيده كلُّ `stop()`، و`_hush` يحلّ وعدَ الموقوف عمداً بـ`false` قبل إيقافه، و`playSequence` جيلٌ واحد يُبطله المتجاوِز، وإعادةُ المحاولة باقيةٌ لعطبها الحقيقيّ. **والفرقُ عن المصدر سطرا تعليقٍ لا شيفرة** (قوبل بايتاً ببايت): نُسب البلاغُ إلى مصدره (`read@9220ab1`) لأنّ البلاغَ عندهم لا عندنا. **وكان قبله حرفياً من `abdf68d090c7`** — أيْ نسخةً حرفيةً من **المعطوبة**، فالعيبُ كان عندنا بعينه. |
| `tools/test_audio_stack.mjs` | مُعدَّل | `d76853f657f1` | الحارسُ السلوكيّ بمشاهده الأربعة كما هو (عنصرُ صوتٍ مزيّف يسجّل ما أُنشئ وما أُوقف). **والفرقُ الوحيد: نصوصُ الفحص تُقرأ من `curriculum.js` بمنطوقها** لا تُكتب بيد — فلا تدخل هذا المستودعَ كلماتُ تطبيقٍ آخر، ومفاتيحُ الفحص مفاتيحُ بنكنا حقاً. |
| `app/index.html` · `app/css/app.css` · `app/js/main.js` | مُعدَّلة | (أجزاءٌ من `9220ab1`) | **قفلُ المقياس ثلاثةَ أجزاء**: ميتا `maximum-scale=1, user-scalable=no` · `touch-action: manipulation` على `html` · إعادةُ إعلان الميتا عند `pageshow`/`visibilitychange`. **وعلّتُه عندنا أثقلُ منها عندهم** ومكتوبةٌ في المواضع الثلاثة: سطحُ الكتابة نقراتٌ متتابعة، ومقياسٌ منحرفٌ يزيح ما تحت الإصبع عمّا تحت القلم. **ولوحُ القلم لم يُمَسّ**: `.pen-surface` تبقى `touch-action: none` وهي أخصُّ فتغلب (`none ∩ manipulation = none`). |
| `app/js/install.js` | مُعدَّل | `728dd6f6a6f9` | الوحدةُ كما هي: `installState` دالّةٌ نقيّة، وفضحُ تنكّر iPadOS بـ`maxTouchPoints`، وخطوتا سفاري، و«افتح في سفاري أولاً»، والصمتُ حيث لا طريق، ولا ظهورَ في المثبَّت، والإغلاقُ يُرقده أسبوعاً. **والتعديلُ اثنان**: مفتاحُ التخزين `uktub.install.v1` (لا `muallim.`) — **ويحرس الفاحصُ تطابقَ سابقته مع `STORE_KEY` في `progress.js` قراءةً من القرص**؛ ونصوصُ الشريط تأخذ اسمَ التطبيق من `BRAND` لا من حرفٍ مكتوب (عهدُ العلامة الواحدة). |
| `tools/test_install.mjs` | مُعدَّل | `3e53f25b5b84` | جدولُ الحالات الستَّ عشرة كما هو. **وزِيدت واحدة**: سابقةُ مفتاح التخزين مقروءةً من `progress.js` — فمفتاحٌ يُنقل بسابقةٍ أجنبية يعيش صامتاً على أجهزة الأطفال ولا يمسكه فحص. |
| `app/css/app.css` — كتلةُ `.install-bar` | مُعدَّلة | (جزءٌ من `260bf48`) | الكتلةُ كما هي، **وزيادةٌ معلَنة**: `.install-x` من ٢٫٧٥rem (٤٤ بكسلاً) إلى ٣rem — و`DESIGN §٤` يشترط ≥٤٨ للهدف الثانويّ. **وهو عيبٌ ثالثٌ قائمٌ في اقرأ نفسِه فيُعاد إليه** (نظيرُ `.btn--ghost` وتشديدِ `do_POST` على `/result`). |
| `tools/test_pwa.mjs` — §٦ | مُعدَّل | (جزءٌ من `9220ab1`) | قسمُ قفل المقياس على نمط قسم اقرأ، **بثلاثة فروق**: زِيد شرطٌ رابع يحرس بقاءَ `touch-action: none` على اللوح (لا موضوعَ له عندهم — لا لوحَ كتابةٍ في اقرأ)؛ **وصفحاتُ التعريف تُجرَد من القرص** لا تُكتب أسماؤها (فصفحتُنا خامسةٌ عندهم أربع، وسادسةٌ غداً تدخل الحراسة يومَ تُكتب)؛ ورقمُ القسم ٦ لا ٥ (٥ عندنا حارسُ أقواس الألواح). |

**ولا صوتَ زِيد ولا `audio_queue.json` مُسّ** في هذا النقل: ما نُقل شيفرةٌ وحرّاس.

**والنقلُ حزمةٌ واحدة بترقيةِ قشرةٍ واحدة** (`sw` إلى `v7`): الثلاثةُ تصل جهازَ
الطفل معاً، وثلاثُ ترقياتٍ متتابعة تُعيد بناءَ القشرة ثلاثاً بلا فائدة.

## ٨. قناةُ الصوت الواحدة (الجلسة ٤ج) — **أوّلُ نقلٍ من غير اقرأ**

بلاغٌ عائليّ من إدارة «اِحْسِبْ» بتكليف المالك (١١ أغسطس ٢٠٢٦): أوّلُ بلاغ ميدانٍ
عندهم — «الصوت متراكب فوق بعضه، مقطوش… البرنامج لا ينتظر الصوت حتى ينتهي فينتقل» —
**وعاش تحت خمسةٍ وستين فحصاً أخضر**، لأنّ حرّاسَ البذرة (وهي بذرتُنا نفسُها) يعدّون
التشغيلات ولا يقيسون التعاقب. فعلاجُه عندهم يُنقل إلينا **قبل أوّل شاشةٍ ناطقة**.

**وهذا أوّلُ مصدرٍ لهذا المستودع من غير اقرأ** — فيُعلَم بعمود مصدره:

| | |
|---|---|
| **المصدر** | `qataruts/calc` — تطبيق «اِحْسِبْ» (لا اقرأ) |
| **الالتزام** | `ad59c56bced23bd117f265a1328b03b7ac52917c` · ١١ أغسطس ٢٠٢٦ · «م١ + مراجعة المدير: قناة صوت واحدة بطابور، والانتقال بعد تمام الكلام — مقبولة» |
| **أصلُ البلاغ** | `calc/docs/FIELD.md §١` (بلاغُ المالك خاماً، وقراءةُ مدير احسب) |
| **تاريخ النقل** | ١١ أغسطس ٢٠٢٦ — الجلسة ٤ج |
| **البصمة** | أولُ ١٢ خانة من `sha256` ملفِّ المصدر عند ذلك الالتزام (كسائر هذا الملف) |

**والمرجعُ التزامٌ لا شجرةُ عمل** — قاعدةٌ رقّاها المدير في مراجعة ٤ب: قُرئت المصادرُ
بـ`git show ad59c56:<ملف>` وقُوبلت بصماتُها، و`../calc` لم يُمَسّ بحرف.

| الملف | الحال | مصدرُه | بصمةُ المصدر | التعديل وعلّته |
|---|---|---|---|---|
| `app/js/audio.js` | مُعدَّل | `calc@ad59c56` `app/js/audio.js` | `9f0cc7ad4166` | **القناةُ بعينها**: `queue` ذيلٌ ينتظره ما بعده · `play()` تصفّ وتُرجع وعداً **يتمّ بـ`ended`** · `stop()` يزيد الجيلَ ويقطع ما يعمل فيُفرغ الطابور · `estimateMs` و`hush` (صمتٌ بزمنه) و`fallback` · وحارسُ التجميد في `playFile` (مهلةٌ من طول الملفّ متى عُرف، ومن طول نصّه قبله). **والفرقُ عن المصدر أربعةٌ، ثلاثةٌ منها شيفرة وكلُّها معلَّلة** (قوبل الملفّان سطراً سطراً): (١) **`playSequence` يلتقط جيلَه ويسقط بتبدّله** — عندهم يمضي التتابعُ لأنّ كلَّ نصٍّ يُصفّ من جديد بجيل اللحظة بعد الإسكات، وعندنا **عهدُ ٤ب** أن نقرةً أثناء التتابع تُبطل بقيّتَه (وإلّا أكملت النصوصُ الباقيةُ فوق الشاشة التي فتحها الطفل) — **وهو موضعُ ائتلاف الطابور والجيل**؛ (٢) **`if (my !== epoch) return false;` في `catch` إعادةِ المحاولة** — بقيّةُ منقول ٤ب: القناةُ تمنع التراكب ولا تمنع البعثَ حين **يتصادف** عطبُ تحميلٍ حقيقيّ مع نقرةِ طفلٍ تنقل؛ (٣) **`hush()` لا تمحو `endCurrent` إلا إن كان لها** (`endCurrent === done`) — وإلّا محا الصامتُ الراحلُ مقبضَ إسكاتِ مَن بعده؛ (٤) اسمُ `playNow` بقي `playAs` (اسمُ البذرة عندنا)، ونسبُ البلاغ مكتوبٌ في رأس القناة إلى أهله (احسب) وإلى بلاغ ٤ب (اقرأ) — فالبلاغان عندهم لا عندنا. **وشهادةٌ تُقيَّد ولا يُبنى عليها**: شجرةُ عمل احسب الحيّة (لا التزامٌ مسمّى) تحمل اليومَ الزيادتين ١ و٢ بأعيانهما تحت عنوان جلستهم «م٢» — بلغاهما من طريق اقرأ، وبلغناهما من طريق ٤ب. |
| `tools/check_speech.mjs` | **بابٌ واحد من ستّة** | `calc@ad59c56` `tools/check_speech.mjs` | `312e32291e06` | نُقل **البابُ السادس وحدَه** («القناةُ واحدة») بشقّيه: النصيُّ (لا صوتَ خارج القناة) والعمليُّ (`channelGate` في node بعنصرٍ وهميّ). **وأبوابُهم الخمسة الأُوَل مؤجَّلة** — موضوعُها بروتوكولُ الصوت (`audio_queue.json` وإعلانُ `SPOKEN`)، ولا نصَّ منطوقاً عندنا بعد ولا قائمةَ على القرص. **وتُطالِب من نفسها**: يومَ تُنشأ القائمةُ أو تُعلن أوّلُ وحدةٍ `SPOKEN` يحمرّ الفاحصُ مطالباً بنقلها (نمطُ الجلسة ٠). **وثلاثةُ فروقٍ في الباب المنقول**: (١) قاعدةُ `.play()` **لا تُحرَّم عندنا مطلقاً** — عندهم لا `.play()` إلا للصوت، وعندنا `penSurface().play()` **حركةُ رسمٍ لا صوت**؛ فصارت الرخصةُ **مشتقّةً من الملفّ** (ما أُسنِد إليه `penSurface(` ومَن أُسنِد إليه، بجولاتٍ) لا قائمةَ أسماءٍ تشيخ؛ (٢) زِيدت `createElement('audio')` و`AudioContext` إلى أبواب التسلّل؛ (٣) **`channelGate` يقيس بمدّتين مختلفتين** لا بمدّةٍ واحدة — ومدّةٌ واحدة تستر مَن انتظر مهلةً ثابتة (يكفيه أن تكون مهلتُه أطولَ منها)، **وأمسك ذلك الفرقُ الزرعَ الثاني في التجربة السالبة**؛ وزِيد حكمان لعهد ٤ب (نقرتان متسابقتان · تتابعٌ تتجاوزه نقرة). |
| `tools/browser_test.html` — §٧ | مُعدَّل | `calc@ad59c56` `tools/browser_test.html` | `8e4278906940` | **الأحكامُ الزمنية**: شاهدُ صوتٍ يسجّل لحظتَي بدء كل تشغيلٍ وتمامه، و`overlaps()` **مقسومةٌ على المستندات** (درسُ احسب الثاني: نافذةُ الفحص ونافذةُ التطبيق نافذتان في اختبار لا صوتان في أذن طفل — **كي لا يحمرّ الحارسُ على الصواب**)، ومدّةُ الملفّ الوهميّ ٩٠٠ مللي فوق أطولِ مهلةٍ ثابتة في شاشاتنا. **والفرقُ الأكبر**: عندهم تُقاس القناةُ على **محطةٍ ناطقة في مسار الطفل**، ولا شاشةَ ناطقةً عندنا بعد — **فتُساق القناةُ نفسُها في متصفّحٍ حقيقيّ** (تُستورَد `js/audio.js` في صفحة الفحص وتُقاس بمؤقّتات المتصفّح)، ويبقى حكمُ «لا صوتان معاً في نافذة الطفل» **نائماً يستيقظ من تلقائه** يومَ تنطق أولى الشاشات. |
| `tools/test_audio_stack.mjs` | مُعدَّل (منقولُ ٤ب) | — | — | **لم يتبدّل عهدُه بحرف، وتبدّل مَن يقوله**: نقرةُ الطفل صارت `stop()` ثم `play()` بعد أن كانت مضمرةً في `play()` — والمشاهدُ الأربعة بأحكامها كما هي. **وزِيد مشهدٌ خامس** يحرس الفرقَ نفسَه: جملتان يقولهما التطبيقُ تُسمَعان كلتاهما تباعاً — فلو عاد الإسكاتُ المضمَر إلى `play()` غداً سقط هذا المشهدُ وحدَه ونجا ما قبله. |
| `app/sw.js` | مُعدَّل | — | — | `VERSION` إلى **`v8`** — ملفُّ قشرةٍ واحدٌ تبدّل (`audio.js`) ولا وحدةَ زِيدت إلى `SHELL`. |

**ولا صوتَ زِيد**: `app/audio/` لا وجودَ له بعد، ولا `audio_queue.json` في المستودع،
وصفرُ نصٍّ منطوقٍ جديد — والنصوصُ الواردة في الحرّاس **حروفٌ هجاء تُقاس بها المؤقّتات
داخل الفاحصَين** لا نصوصَ شاشة، ولا تدخل بنكاً ولا قائمة.
